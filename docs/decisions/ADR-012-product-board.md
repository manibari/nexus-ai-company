# ADR-012: Product Board (產品開發看板)

## 狀態
已採納

## 背景

系統已有多個管理看板：
- **Sales Board** (ADR-011)：商機管理、MEDDIC 分析
- **Project Board** (ADR-010)：專案執行、Goal → Phase → Checkpoint

但缺少針對**產品開發生命週期**的管理工具，包括：
- 功能需求從 Backlog 到上線的追蹤
- QA 測試結果記錄
- UAT (CEO 驗收) 流程
- 版本 Roadmap 管理

## 決策

建立獨立的 **Product Board**，專注於產品開發流程管理。

### 產品開發階段 (P1-P6)

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Product Pipeline                               │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  [P1 Backlog] → [P2 Spec] → [P3 Dev] → [P4 QA] → [P5 UAT] → [P6 Done]│
│      ↓            ↓           ↓          ↓          ↓          ↓     │
│   需求池       規格確認     開發中     內部測試   CEO 驗收   上線完成 │
│                                                                       │
│                              ↓                                        │
│                          [Blocked]                                    │
│                            阻擋中                                     │
└──────────────────────────────────────────────────────────────────────┘
```

### 階段說明

| 階段 | 說明 | 負責 | 進入條件 |
|------|------|------|----------|
| P1 Backlog | CEO 想法、需求池 | CEO | - |
| P2 Spec Ready | 規格已確認 | ORCHESTRATOR | 描述已填寫 |
| P3 In Progress | 開發中 | BUILDER | 規格文件 + 驗收標準 |
| P4 QA Testing | 內部測試 | INSPECTOR | 已指派開發人員 |
| P5 UAT | CEO 驗收 | CEO | QA 測試全部通過 |
| P6 Done | 上線完成 | - | CEO 驗收通過 |
| Blocked | 被阻擋 | - | 任何階段可進入 |

### 資料模型

```python
class ProductStage(Enum):
    P1_BACKLOG = "backlog"
    P2_SPEC_READY = "spec_ready"
    P3_IN_PROGRESS = "in_progress"
    P4_QA_TESTING = "qa_testing"
    P5_UAT = "uat"
    P6_DONE = "done"
    BLOCKED = "blocked"

class ProductType(Enum):
    FEATURE = "feature"          # 新功能
    ENHANCEMENT = "enhancement"  # 功能增強
    BUG_FIX = "bug_fix"         # Bug 修復
    TECH_DEBT = "tech_debt"     # 技術債務
    EXPERIMENT = "experiment"    # 實驗性功能

class ProductPriority(Enum):
    CRITICAL = "critical"
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"

@dataclass
class ProductItem:
    id: str                      # PROD-{year}-{xxxx}
    title: str
    description: str
    type: ProductType
    priority: ProductPriority
    stage: ProductStage

    # 規格
    spec_doc: Optional[str]
    acceptance_criteria: List[str]

    # 指派
    assignee: Optional[str]      # BUILDER, INSPECTOR

    # QA 結果
    qa_results: List[QAResult]

    # UAT 回饋
    uat_feedback: List[UATFeedback]

    # 版本
    version: Optional[str]
    target_release: Optional[str]
```

### API 設計

```yaml
# Product CRUD
POST   /api/v1/product              # 建立產品項目
GET    /api/v1/product              # 列表（支援篩選）
GET    /api/v1/product/{id}         # 取得詳情
PUT    /api/v1/product/{id}         # 更新
DELETE /api/v1/product/{id}         # 刪除

# Stage Management
POST   /api/v1/product/{id}/advance # 推進階段
POST   /api/v1/product/{id}/assign  # 指派 Agent
POST   /api/v1/product/{id}/block   # 標記阻擋
POST   /api/v1/product/{id}/unblock # 解除阻擋

# QA & UAT
POST   /api/v1/product/{id}/qa      # 新增 QA 結果
POST   /api/v1/product/{id}/uat     # 新增 UAT 回饋

# Dashboard
GET    /api/v1/product/dashboard    # 儀表板摘要
GET    /api/v1/product/roadmap      # 版本 Roadmap
GET    /api/v1/product/statistics   # 統計資訊
```

### 前端 UI

Kanban 風格看板，6 欄位對應 6 個階段：

```
┌─────────────────────────────────────────────────────────────────────────┐
│  🏭 Product Board                                    [+ New Item]        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────┐ │
│  │ Backlog │ │  Spec   │ │   Dev   │ │   QA    │ │   UAT   │ │  Done │ │
│  │   (5)   │ │   (2)   │ │   (3)   │ │   (1)   │ │   (1)   │ │  (10) │ │
│  ├─────────┤ ├─────────┤ ├─────────┤ ├─────────┤ ├─────────┤ ├───────┤ │
│  │ ┌─────┐ │ │ ┌─────┐ │ │ ┌─────┐ │ │ ┌─────┐ │ │ ┌─────┐ │ │       │ │
│  │ │Card │ │ │ │Card │ │ │ │Card │ │ │ │Card │ │ │ │Card │ │ │  ...  │ │
│  │ └─────┘ │ │ └─────┘ │ │ └─────┘ │ │ └─────┘ │ │ └─────┘ │ │       │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └───────┘ │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 與其他系統整合

```
CEO Inbox → GATEKEEPER 識別為 project
    ↓
ORCHESTRATOR 建立 Goal（Project Board）
    ↓
對應 ProductItem 建立（Product Board）
    ↓
BUILDER 開發 → INSPECTOR 測試
    ↓
CEO UAT 驗收
    ↓
上線（P6 Done）
```

## 後果

### 優點
- 完整追蹤產品開發生命週期
- QA/UAT 流程標準化
- 與 Project Board 互補（Goal 是執行，Product 是產出）
- Kanban 視覺化易於理解

### 缺點
- 增加系統複雜度
- 需要維護 Product 與 Goal 的關聯

## 實作

| 檔案 | 說明 |
|------|------|
| `backend/app/product/models.py` | 資料模型 |
| `backend/app/product/repository.py` | In-memory Repository |
| `backend/app/api/product.py` | API Router |
| `frontend/src/components/ProductBoard.tsx` | Kanban UI |

## 參考

- ADR-010: Goal-Driven Execution
- ADR-011: Sales Pipeline
- docs/pipelines/product-pipeline.md
